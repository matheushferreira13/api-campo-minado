{
  "name": "api-campo-minado",
  "version": "1.0.0",
  "main": "src/app.js",

  "scripts": {
    "dev": "nodemon src/app.js",
    "start": "node src/app.js",
    "db:create": "PGPASSWORD=${DB_PASSWORD:-postgres} psql -U ${DB_USER:-postgres} -h ${DB_HOST:-localhost} -p ${DB_PORT:-5432} -d ${DB_NAME:-campo_minado} -f schema.sql"
  },

  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1"
  },

  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}